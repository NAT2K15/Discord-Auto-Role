const config = {
    // Discord Token \\
    token: "discord token",
    command: "?createautorole",


    // Embed Settings \\
    embed: {
        color: "BLACK",
        footer: "NAT AUTOROLE"
    },

    // Perms \\ 
    perms: "MANAGE_ROLES"
}

module.exports = config